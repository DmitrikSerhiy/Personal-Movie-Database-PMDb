import { CustomeDecimalPipePipe } from './custome-decimal-pipe.pipe';

describe('CustomeDecimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeDecimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
